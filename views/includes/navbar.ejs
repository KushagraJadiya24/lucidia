<nav class="navbar navbar-dark bg-transparent px-4 pt-3 d-flex justify-content-between align-items-center">
  <a class="navbar-brand fw-bold fs-4" id="main-logo-text" href="/">Lucidia</a>

  <% if (!currentUser) { %>
    <a href="/login" class="btn btn-outline-light">Login</a>
  <% } else { %>
    <div class="d-flex align-items-center gap-3">
      <a href="/entries" class="btn btn-light text-dark fw-semibold px-3">
        <i class="bi bi-journal-text me-1"></i> Entries
      </a>
      <form action="/logout" method="GET" class="d-inline">
        <button class="btn btn-outline-light">Logout</button>
      </form>
    </div>
  <% } %>
</nav>
